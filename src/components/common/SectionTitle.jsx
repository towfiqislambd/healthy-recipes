const SectionTitle = ({ title }) => {
  return (
    <h2 className=" font-merriweather text-[24px] lg:text-[28px] 2xl:text-[32px] 3xl:text-[40px] leading-[140%] font-bold text-center">
      {title}
    </h2>
  );
};

export default SectionTitle;
